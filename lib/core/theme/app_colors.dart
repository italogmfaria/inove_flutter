import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF204B9E);
  static const Color tertiaryColor = Color(0xFF04213F);
  static const Color secondaryColor = Color(0xFFEEF2FF);

  // Background colors
  static const Color backgroundColor = Color(0xFFFFFFFF);
  static const Color accentColor = Color(0xFFF8FAFC);

  // Button colors
  static const Color primaryButton = Color(0xFFF59E0B);

  // Input colors
  static const Color inputColor = Color(0xFF64748B);
  static const Color borderColor = Color(0xE2ADB4C0);

  // Text colors
  static const Color textColor = Color(0xFF000000);

  // Status colors
  static const Color checkColor = Color(0xFF22C55E);
  static const Color errorColor = Color(0xFFC52222);
}
